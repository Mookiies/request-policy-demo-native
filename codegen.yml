overwrite: true
config:
  withHooks: true
  gqlImport: urql#gql
  documentMode: documentNode
generates:
  interface.ts:
    schema:
      - https://api.spacex.land/graphql/
    plugins:
      - typescript
